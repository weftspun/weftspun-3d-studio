# RFD 0120 split every deployed app into its own repository
# (weftspun-studio, weftspun-character-taxonomy, weftspun-usd-viewer),
# each with its own .gitignore for its own build artifacts. This file
# covers only 3d_studio/, android-xr-face-bridge/, and top-level
# fetch/build output.

# RFD 0064 (weftspun/request-for-discussion): the Hugging Face
# training dataset, fetched by scripts/fetch-anime-caption-dataset.sh,
# never committed. Moved here from decisions/0064-.../dataset/ when
# decisions/ itself moved to weftspun/request-for-discussion.
/datasets/

# Local CockroachDB runtime data, from whichever app or ad hoc
# `cockroach start-single-node` created it here. Never tracked,
# regardless of which app, if any, lives in this repository.
/.crdb/

.playwright-mcp/
