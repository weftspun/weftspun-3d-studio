---
description: Agent executes everything on DGX and Surface — never hand the user scripts or optional menus
alwaysApply: true
---

# Agent run instructions (all tasks, all sessions)

**Standing user permission:** If a task is necessary and you can do it — **do it**. Do not ask the user to run commands, scripts, or sync steps you can run yourself.

## Always execute (never delegate)

- Read logs, grep scheduler/API, inspect jobs, SSH between machines
- **New/changed scripts or operator commands → `docs/scripts-cheatsheet.md` then scp sync** (see `.cursor/rules/new-scripts-ops-cheatsheet.mdc`) — same session, no reminder needed
- Run scp sync after other DGX-owned edits (`sync-changes-to-pc.sh`, `sync-to-dgx.ps1`, etc.)
- Restart 3DAIGC-API on DGX, prefetch models, submit test jobs, run `npm test` / anim smoke
- Create/edit files, install deps, fix CI/lint locally
- SessionMem / graphify / memory-bank updates when knowledge must persist

## Never do

- End with “run this command”, “if you want…”, or optional next-step menus
- Hand the user a script block when you have shell access on DGX or Surface
- `git push` / `git pull` / `fetch` / merge `upstream` unless user explicitly asks

## Only ask the user when truly blocked

- Physical device action (Galaxy XR headset, browser click you cannot reach)
- One-time account approval (gated HF model, OAuth in browser)
- Auto-review / approval gate blocks network or credential action
- Secret you cannot read and is not in HF cache / existing `.env`

When blocked: state **what you tried**, **what blocked you**, **exactly what only the user can do** — one step, not a script dump.

## DGX ↔ Surface (equal agents)

| Cursor on | Local root | Reach other |
|-----------|------------|-------------|
| **DGX** | `/home/sifr/Weftspun3DStudio`, `/home/sifr/3DAIGC-API` | `ssh Surface-PC-Tailscale` |
| **Surface** | `C:\Users\alfao\Documents\GitHub\Weftspun3DStudio` | `ssh DGX-Local` |

After edits: **auto-run scp sync** (not git). Say “copied to Surface/DGX” — never “git push” for machine sync.

## Diagnostics before code changes

Follow **`no-guess-use-data.mdc`** and **`remote-log-first.mdc`**: remote logs, smoke (`test:anim-smoke`), `?animSmoke=1`, SSH, git, docs — not guesses.

## Terse or implicit failure signals

User intent may be **one word**, **one sentence**, a **screenshot**, or **implied** (“where is X?”, “still?”, “you didn't sync”). Do **not** require exact phrases like `debug`. If the arc of the conversation is “something should work and doesn't” → run **`terse-debug-ops.mdc`** and **`memory-bank/debug-ops-workflow.md`**: infer → logs → fix → verify → sync — same session.

## Memory on session start (mandatory)

**First turn every session** — run continuity verify, then load memory (do not wait for the user):

```bash
bash scripts/verify-agent-continuity.sh --all-repos
```

Hooks: `.cursor/hooks.json` `sessionStart` / `workspaceOpen` (+ `~/.cursor/hooks.json` on DGX). Rule: `.cursor/rules/agent-continuity-startup.mdc`.

**MindLink** — before other work in this repo:

1. Read `.brain/MEMORY.md` (**Core** + **User Profile**)
2. Read `.brain/SESSION.md` and last 5 entries in `.brain/LOG.md`
3. If MindLink MCP is enabled: use `mindlink_read_memory()` / `mindlink_session_update()` when writing memory

**RepoResident:** `.agent/STATE.md` + `CLAUDE.md`. Then `memory-bank/activeContext.md` when present.

**Code tasks:** follow `.cursor/rules/solid-skills.mdc` and `.agents/skills/solid/` (project rules override strict TDD).

When user says **remember this**: follow `.cursor/rules/remember-this-retention.mdc` **and** promote durable facts to `.brain/MEMORY.md`.

## Surface git from DGX

DGX has GitHub SSH; Surface `origin` is HTTPS. Over Tailscale SSH use:

`ssh Surface-PC-Tailscale "cd /d C:\\Users\\alfao\\Documents\\GitHub\\Weftspun3DStudio && git pull https://github.com/weftspun/weftspun-3d-studio.git main"`

Do not assume Surface has a GitHub SSH key (`git fetch origin` may fail with `Permission denied`).
