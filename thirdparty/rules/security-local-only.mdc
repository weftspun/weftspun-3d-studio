---
description: Prevent committing secrets, env files, and private business documents
alwaysApply: true
---

# Security — never commit or push

Before any git commit or push:

1. **Never stage:** `.env`, `*.pem`, `certs/`, `MONETIZATION_ROADMAP.md`, `Pitch Deck/`, `memory-bank/`, `uploads/`, `.mcp.json`, `.codex/`, `.iwsdk/`, SSH configs with keys — see `.cursor/rules/weftspun-moat-protected.mdc` for moat policy
2. **Use templates only:** `.env.example` with empty placeholders — no real API keys in tracked files
3. **Scripts that write MCP config** (`update-playwright-mcp.*`) must read tokens from **environment variables**, never string literals
4. **Pre-commit hook:** run `sh scripts/install-secret-guard-hook.sh` after clone if hooks are missing
5. **Scan before push:** `bash scripts/scan-repo-secrets.sh .` (PC) or on DGX after sync
6. **GitHub audit (2026-06-24):** Both `AlfaOmegaGrafx/Weftspun3DStudio` and `AlfaOmegaGrafx/3DAIGC-API` `main` are clean — no `.env`/tokens on GitHub. Weftspun stale branches with `.env` were deleted. Canonical: `memory-bank/github-security-audit-2026-06-24.md`. Never commit `~/.config/rp1-spatial-fabric/rp1.env` or real `VITE_*` secrets.

If `git filter-repo` was used, force-push cleaned history only — never push local secret files.

DGX clones must `git fetch weftspun main && git reset --hard weftspun/main` after security updates (backup `.env` first).
