name: GDExtension PR

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  build:
    name: Build
    uses: ./.github/workflows/gdextension-build.yaml
    permissions:
      contents: read
