#usda 1.0
(
    "WARNING: THIS FILE IS GENERATED BY usdGenSchema.  DO NOT EDIT."
)

class Compute_ValueFromJson "Compute_ValueFromJson" (
    customData = {
        string userDocBrief = "This Schema (PrimType) registers a computation that will extract a field value from a JSON string."
    }
)
{
    string inputs:jsonData = "{}" (
        customData = {
            string userDocBrief = "The Json input data, the value shall be extracted from. If this is not valid json there will be no value extracted."
        }
    )
    string jsonPath (
        customData = {
            string userDocBrief = "The path to the field within the json data the value shall be extracted from."
        }
    )
    token jsonValueType (
        allowedTokens = ["none", "float", "double", "string"]
        customData = {
            string userDocBrief = "The value type should match the actual data type that shall be extracted from the json path."
        }
    )
    double outputs:jsonValue:double (
        customData = {
            string userDocBrief = "used as output, when the valueType = double."
        }
    )
    float outputs:jsonValue:float (
        customData = {
            string userDocBrief = "used as output, when the valueType = float."
        }
    )
    string outputs:jsonValue:string (
        customData = {
            string userDocBrief = "used as output, when the valueType = string."
        }
    )
}

class Compute_ScaleDouble "Compute_ScaleDouble" (
    customData = {
        string userDocBrief = "This Schema (PrimType) registers a computation that will multiply the input value with the scalarFactor"
    }
)
{
    double inputs:value = 0 (
        customData = {
            string userDocBrief = "The input value to this computation."
        }
    )
    double outputs:result (
        customData = {
            string userDocBrief = "The result of the computation."
        }
    )
    double scalarFactor = 1 (
        customData = {
            string userDocBrief = "The scalar factor that shall be multiplied to the input value."
        }
    )
}

class "Datasource" (
    customData = {
        string userDocBrief = "Base class for all data source primitives."
    }
)
{
    string outputs:data = "{}" (
        customData = {
            string userDocBrief = "The data output of the data source as JSON string."
        }
    )
}

class MockDatasource_RandomFloat "MockDatasource_RandomFloat" (
    customData = {
        string userDocBrief = "This is an example mock data source that produces random float values."
    }
)
{
    float interval = 1 (
        customData = {
            string userDocBrief = "The update interval in seconds the mock datasource shall provide data and author the \\sa outputs:data attribute in the current usd stage this prim is authored."
        }
    )
    string outputs:data = "{}" (
        customData = {
            string userDocBrief = "The data output of the data source as JSON string."
        }
    )
}

class "CollisionAPI" (
    customData = {
        token[] apiSchemaOverridePropertyNames = ["purpose", "visibility"]
        string userDocBrief = "Collision-related metadata attachable to geometry prims."
    }
)
{
    token[] collision:interactionTypes = ["Collide"] (
        allowedTokens = ["Collide", "Select", "Grab"]
        customData = {
            string userDocBrief = "How colliders participate in interaction."
        }
    )
    token collision:shape = "Box" (
        allowedTokens = ["Box", "Sphere", "Capsule", "Cylinder", "Convex"]
        customData = {
            string userDocBrief = "Collision shape override."
        }
    )
    token collision:type = "Static" (
        allowedTokens = ["Static", "Rigidbody"]
        customData = {
            string userDocBrief = "Collision behavior type."
        }
    )
    token purpose = "guide"
    token visibility = "invisible"
}

class "InteractionAPI" (
    customData = {
        string userDocBrief = "Interaction-related metadata attachable to prims."
    }
)
{
    bool interaction:enabled = 1 (
        customData = {
            string userDocBrief = "Define if colliders participate in interaction."
        }
    )
    bool interaction:highlightable = 1 (
        customData = {
            string userDocBrief = "Define if the asset shall display a highlight effect when selected."
        }
    )
    color3f interaction:highlightColor = (1, 0, 0) (
        customData = {
            string userDocBrief = "Color for the highlight effect."
        }
    )
    uniform token interaction:identifier = "NO_VALUE" (
        customData = {
            string userDocBrief = "Used to link the asset to a specific interaction behavior in the engine."
        }
    )
}

class "CollisionSetAPI" (
    customData = {
        string userDocBrief = "The collision set contains a list of relationships to the actual colliders."
    }
)
{
    rel physics:collider (
        customData = {
            string userDocBrief = "Define which prims are used as colliders."
        }
    )
    rel physics:collider:querry (
        customData = {
            string userDocBrief = "If the collider used for interaction raycasting is different from the ones used for physics simulation, this should be authored here."
        }
    )
}

