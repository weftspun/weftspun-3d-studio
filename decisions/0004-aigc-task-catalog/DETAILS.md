# RFD 0004 details: supported tasks and file references

Supported tasks: text to 3D, image to 3D, image to raw mesh, mesh
painting (text and image), mesh segmentation, mesh retopology, UV
unwrapping, mesh editing (text and image), auto rigging, text to
image, text to motion, image to Gaussian splat, image to world,
environment scan, avatar from image, avatar from photo, image to
layers.

- Catalog: `src/library/aiModelsCatalog.js`
- UI: `src/components/TaskManager.jsx`
- Live list: GET /api/v1/system/models
- Deleted features: issue #6
